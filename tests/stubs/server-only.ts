// Stub vacio de `server-only` para el entorno de tests (Node/Vitest), donde no
// existe el contexto de React Server Components. En la app real, `server-only`
// sigue activo y evita que estos modulos se importen desde el cliente.
export {};
