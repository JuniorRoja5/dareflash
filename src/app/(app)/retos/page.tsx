// STUB (Paso C · unidad 1). Solo el encabezado con el nombre de la pantalla; el feed de Retos se
// diseña en su propia unidad. Sin datos ni componentes inventados.
export const metadata = { title: "Retos · DareFlash" };

export default function RetosPage() {
  return (
    <div className="mx-auto w-full max-w-2xl px-4 py-8">
      <h1 className="text-2xl font-semibold text-text">Retos</h1>
    </div>
  );
}
